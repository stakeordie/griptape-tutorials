import BlockchainModule from './base';
import { ModuleBaseResponse, ModuleSimpleResponse } from './types';
export interface MintParametersResponse extends ModuleBaseResponse {
    result: {
        mint_denom: string;
        inflation_rate_change: string;
        inflation_max: string;
        inflation_min: string;
        goal_bonded: string;
        blocks_per_year: string;
    };
}
export declare class MintingModule extends BlockchainModule {
    getParameters(): Promise<MintParametersResponse>;
    getInflation(): Promise<ModuleSimpleResponse>;
    getAnualProvisions(): Promise<ModuleSimpleResponse>;
}
export declare function useMinting(): MintingModule;
