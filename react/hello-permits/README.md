# Creating permits

This example shows how to create permits and validate them to get your balance.

## Run it locally

### Install dependencies:

```bash
npm install
```

### Serve the app:

```bash
npm start
```
